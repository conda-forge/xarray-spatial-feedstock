{% set python_min = "3.12" %}

{% set name = "xarray-spatial" %}
{% set version = "0.10.17" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 1c8f689aba774fe0e3bb63c0c30d70c2f25218f57c6fc0531e3e79b49f80a48f

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - xrspatial = xrspatial.__main__:main

requirements:
  host:
    - python {{ python_min }}
    - python-build
    - param
    - pyct-core
    - setuptools >=54.1.2
    - setuptools-scm
    - pip
  run:
    - urllib3
    - matplotlib-base
    - zstandard
    - scipy
    - python >={{ python_min }}
    - datashader >=0.15.0
    - numba
    - xarray
    - numpy

test:
  imports:
    - xrspatial
  commands:
    - pip check
    - xrspatial --help
  requires:
    - pip
    - python {{ python_min }}
    - dask
    - pyarrow

about:
  home: https://pypi.org/project/xarray-spatial/
  summary: xarray-based spatial analysis tools
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - brendancol
    - giancastro
    - ianthomas23
    - ocefpaf
    - thuydotm
